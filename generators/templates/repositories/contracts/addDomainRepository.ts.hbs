import { {{pascalCase name}}Data } from "@/entities";

export interface Add{{pascalCase name}}Repository {
  add{{pascalCase name}}({{camelCase name}}: {{pascalCase name}}Data): Promise<{{pascalCase name}}Data | null>;
}