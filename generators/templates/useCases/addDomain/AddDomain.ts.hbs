import { {{pascalCase name}}Data, {{pascalCase name}}Entity } from "../../entities";
import { Add{{pascalCase name}}Repository } from "../../repositories";

export type Add{{pascalCase name}} = (data: {{pascalCase name}}Data) => Promise<{{pascalCase name}}Entity | null>;
export type Add{{pascalCase name}}Signature = (add{{pascalCase name}}Repository: Add{{pascalCase name}}Repository) => Add{{pascalCase name}};

export const add{{pascalCase name}}: Add{{pascalCase name}}Signature =
  (add{{pascalCase name}}Repository: Add{{pascalCase name}}Repository) => (data: {{pascalCase name}}Data) => {
    return add{{pascalCase name}}Repository.add{{pascalCase name}}(new {{pascalCase name}}Entity(data));
  }